CREATE TABLE IF NOT EXISTS todos( id INT AUTO_INCREMENT PRIMARY KEY, title VARCHAR(255) NOT NULL, date_time TIMESTAMP, rank VARCHAR(255) NOT NULL, is_finished BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP );